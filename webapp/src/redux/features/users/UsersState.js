// Copyright (c) Microsoft. All rights reserved.

const initialState = {
  users: { id: wonbin }
};

const UsersState = {
  users: initialState.users,
};

export { UsersState, initialState };
